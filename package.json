{
  "name": "ccc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild js/main.js --bundle --minify --sourcemap --outfile=dist/bundle.js",
    "build:dev": "esbuild js/main.js --bundle --sourcemap --outfile=dist/bundle.js",
    "watch": "npm run build:dev -- --watch",
    "serve": "esbuild js/main.js --bundle --sourcemap --outfile=dist/bundle.js --servedir=. --watch"
  }
}