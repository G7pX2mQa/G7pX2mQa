{
  "name": "ccc",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "node build.mjs build --prod",
    "build:dev": "node build.mjs build --dev",
    "watch": "node build.mjs watch",
    "serve": "node build.mjs serve",
    "prepare": "node scripts/install-hooks.js",
    "install-hooks": "node scripts/install-hooks.js",
    "precommit": "npm run sync-bundles",
    "sync-bundles": "./sync-bundles.sh"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
